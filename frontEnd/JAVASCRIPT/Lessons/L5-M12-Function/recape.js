function doMath(a, b, c, d) {
  const firstMAth = a + b;
  const secondMath = c + d;
  const multiply = firstMAth * secondMath;
  return multiply;
}

const result = doMath(2, 2, 2, 2);
console.log(result);
